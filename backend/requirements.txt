# Core dependencies
fastapi==0.104.0
uvicorn==0.23.2
python-multipart==0.0.6
pydantic==2.4.2
email-validator==2.1.0  # Required for Pydantic email validation
httpx==0.25.0
python-dotenv==1.0.0

# Authentication and security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
bcrypt==3.2.2  # Downgraded for compatibility with passlib

# Database and caching
redis==5.0.1
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# Media processing
ffmpeg-python==0.2.0
yt-dlp>=2023.12.30  # Updated to latest version to fix YouTube download errors

# PyTorch with CUDA 12.6 support for RTX 5090 (sm_120)
# Using the latest stable release that supports RTX 5090
--extra-index-url https://download.pytorch.org/whl/cu126
torch>=2.5.0
torchvision>=0.20.0
torchaudio>=2.5.0

# AI and ML dependencies
numpy>=1.24.0,<2.0.0  # Pin numpy version for compatibility
whisperx==3.3.3
transformers>=4.48.0
accelerate==0.25.0

# Speaker diarization with compatible versions
pyannote.audio==3.1.1  # Use compatible version to avoid model mismatch warnings
speechbrain>=0.5.12  # Ensure speechbrain compatibility